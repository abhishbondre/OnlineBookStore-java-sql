public class Main {
    public static void main(String[] args) {

        // Create a new instance of the Login class to handle user authentication
        Login login = new Login();
        // Display the initial login menu to the user
        login.firstMenu();   
    }
}